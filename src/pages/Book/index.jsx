import React, { useEffect, useState } from "react";

const Book = () => {
  const [podcastInfo, setPodcastInfo] = useState([]);
  useEffect(() => {
    const fetchData = () => {};
  }, []);
  return <div>Book</div>;
};

export default Book;
